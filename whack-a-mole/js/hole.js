var Hole = function(x, y, time) {
    
    var moles = ["mole-10", "mole-20", "mole-30"]
    var xPos = x
    var yPos = y
    var intervalTime = time
    var interval

    this.start = function() {
        //interval = setInterval(, intervalTime)

    }

    function getMole() {
        var mole = Math.floor(Math.random() * moles.length)
        
    }

}